// TODO: implement list view with EmployeeRow component
//
// This component is intentionally a placeholder. When the list/table view
// toggle is implemented in EmployeesPage, replace this with a single-row
// representation of an employee (avatar, name, role, dept, status, location).
//
// Suggested shape:
//   interface EmployeeRowProps { employee: Employee }
//   render <Link> to /employees/:id with horizontal layout

import type { Employee } from '../../types/employee';

interface EmployeeRowProps {
  employee: Employee;
}

export function EmployeeRow(_props: EmployeeRowProps) {
  return null;
}
